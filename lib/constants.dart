import 'package:flutter/material.dart';

const activeCardColor = Color(0xFF1D1E33);
const inactiveCardColor = Color(0xFF111328);

enum Gender { male, female }

const TextStyle cardtextstyle =
    TextStyle(fontFamily: 'Poppins', fontSize: 18, color: Color(0xFF8D8E98));
const TextStyle boldCardHeadings =
    TextStyle(fontFamily: 'Poppins', fontSize: 50, fontWeight: FontWeight.bold);

const Color buttonColor = Color(0xFF4C4F5E);
